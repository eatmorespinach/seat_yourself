<h1>Restaurants#index</h1>
<p>Find me in app/views/restaurants/index.html.erb</p>
<%= link_to "Sign Up", "/user/new" %>
<table>
	<% @restaurants.each do |x| %>
		<tr>
			<td>
				<%= link_to x.name, "/restaurants/#{x.id}" %>
			</td>
		</tr>
	<%end%>
</table>